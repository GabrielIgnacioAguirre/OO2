package io.github.unlp_oo.archetype;

import org.junit.jupiter.api.*;
import static org.junit.jupiter.api.Assertions.*;

public class SistemaTest {
	private Sistema s = new Sistema();
	private Usuario user1 = new Usuario("A");;
	
	@BeforeEach
	void setUp() {
		
	}
	
	@Test
	void testChequearUsuarioExistente(){
		assertEquals(true,(s.CrearUsuario(user1.getScreenName())));
		assertEquals(false,(s.CrearUsuario(user1.getScreenName())));
	}
	
	@Test
	void testEliminarUsuario() {
		assertEquals(true,s.EliminarUsuario("A"));
		assertEquals(false,s.EliminarUsuario("A"));
	}
}
